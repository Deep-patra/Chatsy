import { motion } from 'framer-motion'

interface ITabsProps {
  direction: 'vertical' | 'horizontal'
  children: React.ReactNode
}

export default function Tabs({ direction, children }: ITabsProps) {
  return (
    <div>

    </div>
  ) 
}
